import { atom } from 'jotai'

/**
 * Current number of rows in grid
 */
export const rowsCountAtom = atom(12)

/**
 * Number of columns in grid
 */
export const colsCountAtom = atom(8)

export const imageWidthAtom = atom(1200)